# GitHub 저장소 Private → Public 변경

## 🔄 저장소 공개 설정 변경

### 방법 1: Settings → Danger Zone (추천)

1. **GitHub 저장소 페이지**
   - `optlisting-team/optlisting-` 저장소 접속

2. **Settings 탭**
   - 저장소 상단 탭에서 **"Settings"** 클릭

3. **Danger Zone 찾기**
   - 페이지 맨 아래로 스크롤
   - **"Danger Zone"** 섹션 찾기
   - ⚠️ 빨간색 경고 표시가 있는 섹션

4. **Change visibility**
   - "Change visibility" 또는 "Change repository visibility" 찾기
   - **"Change visibility"** 버튼 클릭

5. **Public으로 변경**
   - "Make public" 선택
   - 확인 메시지에 `optlisting-team/optlisting-` 입력
   - **"I understand, change repository visibility"** 클릭

---

### 방법 2: 직접 링크

1. 저장소 Settings 페이지에서
2. URL에 직접 접속:
   ```
   https://github.com/optlisting-team/optlisting-/settings
   ```
3. 페이지 하단 "Danger Zone" 섹션으로 스크롤

---

## ⚠️ 주의사항

### Public으로 변경 시:
- ✅ 모든 코드가 공개됨
- ✅ 누구나 코드를 볼 수 있음
- ✅ Vercel Hobby 플랜으로 배포 가능 (무료)

### 보안 확인:
- [ ] API 키나 비밀번호가 코드에 없는지 확인
- [ ] `.env` 파일이 `.gitignore`에 있는지 확인
- [ ] 민감한 정보가 커밋되어 있지 않은지 확인

---

## 🔒 보안 체크리스트

Public으로 변경하기 전에 확인:

- [ ] `.env` 파일이 `.gitignore`에 포함되어 있나?
- [ ] 환경 변수 파일들이 커밋되지 않았나?
- [ ] API 키나 토큰이 코드에 하드코딩되어 있지 않은가?
- [ ] 데이터베이스 비밀번호가 노출되지 않았나?

---

## 📝 변경 후

### Vercel 배포:
- ✅ Hobby 플랜(무료)으로 배포 가능
- ✅ "Add New Project" → 저장소 선택
- ✅ Public 저장소로 인식됨

---

## 🔄 다시 Private으로 변경하려면

나중에 Private으로 다시 변경 가능:
- Settings → Danger Zone → "Change visibility" → "Make private"

---

## 요약

**위치:**
- GitHub 저장소 → Settings → 맨 아래 "Danger Zone" 섹션

**절차:**
1. Settings 페이지 맨 아래로 스크롤
2. "Danger Zone" 찾기
3. "Change visibility" 클릭
4. "Make public" 선택
5. 저장소 이름 입력 후 확인



