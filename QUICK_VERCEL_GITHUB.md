# Vercel GitHub 권한 빠른 설정

## 🚀 가장 빠른 방법

### Step 1: Vercel에서 바로 연결

1. **Vercel 대시보드 접속**
   - https://vercel.com
   - 로그인 (또는 가입)

2. **"Add New Project" 클릭**
   - 메인 대시보드에서 보임

3. **"Continue with GitHub" 클릭**
   - GitHub 로그인 창이 열림
   - GitHub 로그인

4. **권한 승인**
   - "Authorize Vercel" 또는 "Install" 버튼 클릭
   - ⚠️ **중요**: `optlisting-team` 조직 접근 권한 허용!

5. **저장소 선택**
   - 목록에서 `optlisting-team/optlisting-` 찾기
   - 선택

---

## 🔍 권한 확인 방법

### Vercel에서:
- "Add New Project" → 저장소 목록에 `optlisting-team/optlisting-`가 보이면 ✅

### GitHub에서:
1. Settings → Applications → Installed GitHub Apps
2. Vercel 찾기
3. Configure → Repository access 확인

---

## ⚠️ 조직 저장소가 안 보일 때

### 해결 방법:
1. **조직 접근 권한 확인**
   - Vercel Configure → `optlisting-team` 선택
   
2. **권한 재요청**
   - Vercel에서 저장소 선택 화면
   - "Grant access" 또는 "Configure access" 클릭

3. **조직 설정 확인**
   - GitHub → `optlisting-team` → Settings
   - Third-party access → Vercel 권한 확인

---

## ✅ 확인

**권한이 제대로 설정되면:**
- Vercel → "Add New Project"
- 저장소 목록에 `optlisting-team/optlisting-` 표시
- 선택 가능

---

## 📝 요약

1. Vercel → "Add New Project"
2. "Continue with GitHub"
3. GitHub 로그인
4. 권한 승인 (조직 포함!)
5. 저장소 선택

**가장 간단한 방법: Vercel에서 직접 연결!** 🎯



