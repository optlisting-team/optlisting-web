# Environment Variables Example
# Copy this file to .env.local (for local development) or set these in your deployment platform

# ============================================
# Test Credits Feature Flag
# ============================================
# Enable test credits grant button in the UI
# - Set to 'true' (string) to show the Grant Test Credits button in Credits modal
# - Set to 'false' or leave unset to hide the button
# - This only controls UI visibility; backend still requires ADMIN_API_KEY for actual grant
# - Useful for: Development, testing, staging environments
# - WARNING: Do not enable in production unless absolutely necessary for testing
VITE_ENABLE_TEST_CREDITS=false

# ============================================
# Admin API Key (Optional - for test credits)
# ============================================
# Admin API key for test credits grant endpoint
# - Only needed if VITE_ENABLE_TEST_CREDITS=true
# - Must match ADMIN_API_KEY environment variable on the backend
# - Backend validates this key before granting credits
# - WARNING: This is exposed in the frontend bundle - use with caution
# - For production: Consider using session-based auth instead
VITE_ADMIN_API_KEY=

# ============================================
# API Base URL (Optional)
# ============================================
# Backend API base URL
# - For development: Set to your local backend URL (e.g., http://localhost:8000)
# - For production: Leave empty to use relative paths (proxied by vercel.json)
# - If empty, all API calls will use relative paths (/api/*)
VITE_API_URL=
